# AAP — TP01 : Python

## Objectifs

À l'issue de ce TP, vous devez savoir :

- écrire une fonction Python ;
- utiliser des listes ;
- gérer une erreur avec une exception ;
- écrire des tests avec `pytest` ;
- exécuter les tests depuis le terminal ;
- vérifier la qualité du code avec `ruff`.

---

## Exercice 1 — Calculer une moyenne

On souhaite écrire une fonction permettant de calculer
la moyenne des valeurs contenues dans une liste.

La fonction doit avoir la forme :

```python
def moyenne(valeurs):
    ...# AAP — TP01 : Python

## Objectifs

À l'issue de ce TP, vous devez savoir :

- écrire une fonction Python ;
- utiliser des listes ;
- gérer une erreur avec une exception ;
- écrire des tests avec `pytest` ;
- exécuter les tests depuis le terminal ;
- vérifier la qualité du code avec `ruff`.

---

## Exercice 1 — Calculer une moyenne

On souhaite écrire une fonction permettant de calculer
la moyenne des valeurs contenues dans une liste.

La fonction doit avoir la forme :

```python
def moyenne(valeurs):
    ...